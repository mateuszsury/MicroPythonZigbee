# third_party

This directory is intentionally excluded from git tracking.

Use:

```bash
bash tools/bootstrap_third_party.sh
```

to fetch pinned dependencies and apply required `uzigbee` vendor overrides.
